name: 'Tag'
description: 'Create a tag from the version from the package.json that was modified'
inputs:
    headref:
        description: 'headref'
    refname:
        description: 'refname'
    sha:
        description: 'sha'
runs:
    using: 'node16'
    main: 'index.js'
